# Satscard Private Key Exposer

Used to get the private key from a Satscard. Meant for use with freewallet.io.

## Build

```bash
npx react-native build-android --mode=release
cd ./android
./gradlew assembleRelease
```
